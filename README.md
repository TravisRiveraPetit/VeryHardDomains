# VeryHardDomains
A collection of very hard PDDL problems.

# Usage
`python3 generate.py n` generates instances `p1.pddl ... pn.pddl`.
Each instance `p_i` is typically harder than `p_i-1`.  All generators are deterministic.
